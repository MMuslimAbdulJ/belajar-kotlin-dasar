fun main() {

    val names : Array<String> = arrayOf("Muslim", "Ikhlash", "Danny")
    for(name in names) {
        println(name)
    }

    for(value in 1..100) {
        println(value)
    }

    for(value in 100 downTo 1 step 2 ) {
        println(value)
    }
    
}